# ── Supabase ────────────────────────────────────────────────────────────────
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ── Server ───────────────────────────────────────────────────────────────────
PORT=3000

# Comma-separated list of allowed frontend origins (use * for dev only)
# Example: CORS_ORIGIN=https://chesster-lovat.vercel.app
CORS_ORIGIN=*

# ── Blockchain / Escrow ──────────────────────────────────────────────────────
# RPC endpoint for the target network (Sepolia example uses Infura/Alchemy)
RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# Address of the deployed ChessterEscrow contract
# Populated after running: forge script script/DeployEscrow.s.sol --rpc-url $RPC_URL --broadcast
ESCROW_CONTRACT_ADDRESS=0x

# Private key of the coordinator wallet (the deployer becomes coordinator)
# Fund this wallet with a small amount of Sepolia ETH for gas.
# WARNING: keep this secret — never commit to git.
COORDINATOR_PRIVATE_KEY=0x
