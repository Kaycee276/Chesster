# Backend API URL (include trailing slash)
VITE_BACKEND_URL=http://localhost:3000/

# Reown AppKit project ID (required for MetaMask / wallet connection)
VITE_PROJECT_ID=your_reown_project_id_here

# Deployed ChessterEscrow contract address on Sepolia (or your target network)
# Players need to call token.approve(VITE_ESCROW_CONTRACT_ADDRESS, amount) before wagering.
# Populated after: forge script script/DeployEscrow.s.sol --rpc-url $SEPOLIA_RPC_URL --broadcast
VITE_ESCROW_CONTRACT_ADDRESS=0x

# Public RPC URL for read-only contract queries from the frontend (no API key needed for drpc)
VITE_RPC_URL=https://sepolia.drpc.org
